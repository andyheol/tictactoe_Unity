using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class text : newV
{
    /*
    GameObject distance;
    int a = 0;
    int b = 0;
    float k = 0;
    // Start is called before the first frame update
    void Start()
    {
        distance = GameObject.Find("Text");
        distance.GetComponent<Text>().text = "x:"+"0"+"  "+"o:"+"0";
    }

    // Update is called once per frame
    void Update()
    {
        k+=Time.deltaTime;
        if (k > 1.0f)
        {
            a = xpoint;
            b = opoint;
             //  a +=1;
             //  b +=1;
            distance.GetComponent<Text>().text = "x:" + a+" "+ "o:" + b;
            print("a: " + a + " b: " + b);
            k = 0;
        }
    }
*/
}
